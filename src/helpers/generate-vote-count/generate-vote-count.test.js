//@flow
import { generateVoteCount } from './generate-vote-count';

describe('generateVoteCount', () => {
	test('It should return a number between 0 and 66', () => {
		expect(generateVoteCount()).toBeGreaterThan(0);
		expect(generateVoteCount()).toBeLessThan(66);
	});

	test('It should return a number between 0 and 66', () => {
		expect(generateVoteCount()).toBeGreaterThan(0);
		expect(generateVoteCount()).toBeLessThan(66);
	});

	test('It should return a number between 0 and 66', () => {
		expect(generateVoteCount()).toBeGreaterThan(0);
		expect(generateVoteCount()).toBeLessThan(66);
	});

	test('It should return a number between 0 and 66', () => {
		expect(generateVoteCount()).toBeGreaterThan(0);
		expect(generateVoteCount()).toBeLessThan(66);
	});

	test('It should return a number between 0 and 66', () => {
		expect(generateVoteCount()).toBeGreaterThan(0);
		expect(generateVoteCount()).toBeLessThan(66);
	});

	test('It should return a number between 0 and 66', () => {
		expect(generateVoteCount()).toBeGreaterThan(0);
		expect(generateVoteCount()).toBeLessThan(66);
	});

	test('It should return a number between 0 and 66', () => {
		expect(generateVoteCount()).toBeGreaterThan(0);
		expect(generateVoteCount()).toBeLessThan(66);
	});

	test('It should return a number between 0 and 66', () => {
		expect(generateVoteCount()).toBeGreaterThan(0);
		expect(generateVoteCount()).toBeLessThan(66);
	});

	test('It should return a number between 0 and 66', () => {
		expect(generateVoteCount()).toBeGreaterThan(0);
		expect(generateVoteCount()).toBeLessThan(66);
	});

	test('It should return a number between 0 and 66', () => {
		expect(generateVoteCount()).toBeGreaterThan(0);
		expect(generateVoteCount()).toBeLessThan(66);
	});
});
